query commodity($id: String!) {
    commodity(id: $id) {
        id
        name
        description
        skus {
            id
            name
            description
            images {
                imageUri
            }
            unit
            price
        }
    }
}
