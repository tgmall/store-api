query commodity($id: String!) {
    commodity(id: $id) {
        id
        name
        description
        skus {
            id
            name
            description
            images {
                smallImageUrl
                largeImageUrl
            }
            unit
            price
        }
    }
}
