mutation addCartItem($skuId: String!, $skuAmount: BigDecimal!) {
    addCartItem(skuId: $skuId, skuAmount: $skuAmount) {
        id
        items {
            id
            skuId
            skuAmount
        }
    }
}
