mutation($input: UserCreateInput!) {
    createUser(input: $input) {
        id
        username
        nickName
        mobile
        avatarUrl
        subject {
            id
            name
            type
            contact {
                id
                name
                mobile
                address
                postcode
            }
        }
    }
}
